/* const User = require ('../models/user.js');

const kont = User.findById(sess.userID, async function(err, doc) {
  if(err) {
    console.log(err)
  } else {
    console.log(doc.userAccount)
  }
}) */
//"Dus hier auch nutzlos" - Serhat